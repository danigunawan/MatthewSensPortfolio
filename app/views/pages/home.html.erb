<main role="main" class="inner cover">
  <div id="text-highlight">
    <h1 class="cover-heading"><span class="element"></span> <span class="highlight-name">Matthew Israelson</span>. I'm a <span class="alpha">full-stack</span> web developer.</h1>
      <!-- <a href="<%= about_me_path %>" class="btn btn-lg btn-secondary">Learn more</a> -->
    <div class="gradient-button-container">
      <%= link_to "View my work", portfolios_path, class: "hoverbutton gradient-button" %>
    </div>
    <!-- </p> -->
  </div>
</main>

<%= render "about_me" %>

<%= render "skills" %>

<hr>
<h1 class="cover-heading">Tech Tweets => Pulled from the Twitter API</h1>

  <div class="row sortable">
    <% @tweets.each do |tweet| %>
      <div class="col-auto col-sm-6">
        <div class="sortable-card mb-4 box-shadow">
          <div class="card-body">
            <h3 class="card-title"><%= tweet %></h3>
          </div>
        </div>
      </div>
    <% end %>
  </div>

<%= render "sortable_blocks" %>

<!-- <%= render partial: 'pages/sortable_blocks', collection: @portfolio_items %> -->
